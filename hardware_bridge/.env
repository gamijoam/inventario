# Configuración del Hardware Bridge - WebSocket Client

# URL del VPS (WebSocket)
VPS_URL=wss://demo.invensoft.lat

# ID único de este cliente (identificador de la caja)
CLIENT_ID=caja-1

# Modo de Impresora: "WINDOWS", "USB" (Zadig) o "VIRTUAL" (Pruebas)
PRINTER_MODE=VIRTUAL

# Nombre exacto de la impresora en Windows (Panel de Control -> Impresoras)
# Ejemplo: "POS-58", "EPSON TM-T20II Receipt", etc.
PRINTER_NAME=POS-58
