*.zip filter=lfs diff=lfs merge=lfs -text

# Auto detect text files and normalize line endings to LF
* text=auto

# Force LF for build outputs (prevents warnings on Windows)
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
*.json text eol=lf

# Force CRLF for Windows batch files
*.bat text eol=crlf
*.cmd text eol=crlf
